{
  "name": "@sharmeats/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Pure-TS shared kernel: order status machine, fee constants, domain enums. Imported by all four surfaces; mirrors the server RPC authority.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./order-status": "./src/order-status.ts",
    "./fees": "./src/fees.ts"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  }
}
