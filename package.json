{
  "name": "sharmeats",
  "version": "0.1.0",
  "private": true,
  "description": "Sharm Eats — multi-category delivery super-app for Sharm el-Sheikh (food first). Monorepo: customer + driver apps (Expo), merchant + admin dashboards (Next.js), shared packages, Supabase backend.",
  "comment:workspaces": "Only shared pure-TS packages are workspaces. The apps (Expo customer/driver, Next.js merchant/admin) and the landing site each manage their OWN node_modules + lockfile, because Expo (React 18) and Next.js (React 19) need different, co-located React versions that npm hoisting would conflict over. Install each app from its own directory.",
  "workspaces": [
    "packages/*"
  ],
  "comment:scripts": "The app shortcuts cd because the apps are NOT workspaces (see comment:workspaces) — `--workspace apps/...` fails with ENOWORKSPACES.",
  "scripts": {
    "customer": "cd apps/customer && npm run start",
    "driver": "cd apps/driver && npm run start",
    "merchant": "cd apps/merchant-web && npm run dev",
    "admin": "cd apps/admin-web && npm run dev",
    "landing": "cd landing && npm run dev",
    "db:types": "supabase gen types typescript --project-id ${SUPABASE_PROJECT_REF:-ilqpsebcfbaoaogimhud} --schema public > packages/db-types/database.types.ts",
    "db:diff": "supabase db diff",
    "db:push": "supabase db push"
  },
  "engines": {
    "node": ">=20"
  }
}
